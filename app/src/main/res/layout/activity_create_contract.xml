<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://android.com"
    xmlns:tools="http://android.com"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#F7F9F8">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="20dp">

        <!-- Toolbar giả -->
        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="24dp">

            <ImageButton
                android:id="@+id/btnBack"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:src="@drawable/ic_back"
                app:tint="#1B4332" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_centerInParent="true"
                android:text="Tạo hợp đồng"
                android:textColor="#1B4332"
                android:textSize="20sp"
                android:textStyle="bold" />
        </RelativeLayout>

        <!-- Chủ hợp đồng -->
        <TextView
            android:text="Chủ hợp đồng"
            android:textStyle="bold"
            android:textColor="#333333"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content" />

        <EditText
            android:id="@+id/edtOwner"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:layout_marginTop="8dp"
            android:layout_marginBottom="16dp"
            android:background="@drawable/bg_input_green"
            android:drawableStart="@drawable/ic_user"
            android:drawablePadding="12dp"
            android:padding="12dp"
            android:text="Nguyễn Văn A" />

        <!-- Ngày bắt đầu -->
        <TextView
            android:text="Ngày bắt đầu (1–28)"
            android:textStyle="bold"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content" />

        <EditText
            android:id="@+id/edtStartDate"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:layout_marginTop="8dp"
            android:layout_marginBottom="16dp"
            android:background="@drawable/bg_input_green"
            android:drawableStart="@drawable/ic_calendar"
            android:drawablePadding="12dp"
            android:focusable="false"
            android:padding="12dp"
            android:text="01/04/2026" />

        <!-- Ngày kết thúc -->
        <TextView
            android:text="Ngày kết thúc"
            android:textStyle="bold"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content" />

        <EditText
            android:id="@+id/edtEndDate"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:layout_marginTop="8dp"
            android:layout_marginBottom="16dp"
            android:background="@drawable/bg_input_green"
            android:drawableStart="@drawable/ic_calendar"
            android:drawablePadding="12dp"
            android:focusable="false"
            android:padding="12dp"
            android:text="31/03/2027" />

        <!-- Giá thuê -->
        <TextView
            android:text="Giá thuê (đ/tháng)"
            android:textStyle="bold"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content" />

        <EditText
            android:id="@+id/edtPrice"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:layout_marginTop="8dp"
            android:layout_marginBottom="16dp"
            android:background="@drawable/bg_input_green"
            android:drawableStart="@drawable/ic_money"
            android:drawablePadding="12dp"
            android:padding="12dp"
            android:inputType="number"
            android:text="8.500.000" />

        <!-- Note Box -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@drawable/bg_note_light_green"
            android:orientation="horizontal"
            android:padding="12dp"
            android:layout_marginBottom="24dp">

            <ImageView
                android:layout_width="20dp"
                android:layout_height="20dp"
                android:src="@drawable/ic_info"
                app:tint="#2D6A4F" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="8dp"
                android:text="Ngày thanh toán = ngày 1 hàng tháng (=\nngày bắt đầu HĐ)"
                android:textColor="#2D6A4F"
                android:textSize="14sp" />
        </LinearLayout>

        <!-- Upload Section -->
        <LinearLayout
            android:id="@+id/layoutUpload"
            android:layout_width="match_parent"
            android:layout_height="140dp"
            android:background="@drawable/bg_dashed_green"
            android:gravity="center"
            android:orientation="vertical">

            <ImageView
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:src="@drawable/ic_upload"
                app:tint="#52B788" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:text="Tải lên PDF hợp đồng"
                android:textColor="#1B4332"
                android:textStyle="bold" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Upload lên Cloudinary"
                android:textColor="#888888"
                android:textSize="12sp" />
        </LinearLayout>

        <!-- Nút Tạo hợp đồng -->
        <Button
            android:id="@+id/btnCreate"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:layout_marginTop="32dp"
            android:backgroundTint="#1B4332"
            android:text="Tạo hợp đồng"
            android:textAllCaps="false"
            android:textSize="16sp"
            app:cornerRadius="12dp" />

    </LinearLayout>
</ScrollView>
